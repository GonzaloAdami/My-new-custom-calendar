export class Nota {
    dia: string;
    nota: string;
    id: number;
  
    constructor(dia: string, nota: string, id: number) {
      this.dia = dia;
      this.nota = nota;
      this.id = id;
    }
  }